import { StyleSheet } from "react-native";

export const postsIntroOverlayStyles = StyleSheet.create({
  root: {
    position: "absolute",
    zIndex: 10,
  },
  image: {
    width: "100%",
    height: "100%",
  },
});
